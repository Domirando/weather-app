import {LOCATION_CHANGED} from './actionType'
// eslint-disable-next-line no-unused-vars
let reducer = (state=[], action) => {
    switch(action.type){
        case LOCATION_CHANGED:
        default:
            return state
    }
}
export default reducer